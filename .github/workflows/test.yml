name: Tests
on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'

      - run: npm install
      - run: npm test

      - run: ls
      - run: pwd
      - run: .github/workflows/check-status.sh


      # TODO: test the build itself, and the diff with the dist folder
